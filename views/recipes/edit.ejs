<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Recipe Book</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css"
    />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
  </head>
  <body>
    <div class="container">
      <h1>Edit Recipe Page</h1>
      <form
        class="col s12"
        action="/recipes/<%=recipe.id%>?_method=PUT"
        method="POST"
      >
        <div class="row">
          <div class="input-field col s6">
            <input type="text" name="name" value="<%=recipe.name%>" />
            <label class="active" for="name">Name</label>
          </div>
          <div class="input-field col s6">
            <input type="text" name="img" value="<%=recipe.img%>" />
            <label class="active" for="img">Link to Image</label>
          </div>
        </div>
        <div class="row">
          <div class="input-field col s12">
            <textarea name="ingredients" class="materialize-textarea">
<%=recipe.ingredients%></textarea
            >
            <label class="active" for="ingredients"
              >Ingredient List: (Separate with commas)</label
            >
          </div>
        </div>
        <div class="row">
          <div class="input-field col s12">
            <textarea name="steps" class="materialize-textarea">
<%=recipe.steps%></textarea
            >
            <label class="active" for="steps"
              >Directions: (Separate steps with >>)</label
            >
          </div>
        </div>
        <div class="row">
          <div class="input-field col s6">
            <input type="text" name="tags" value="<%=recipe.tags%>" />
            <label class="active" for="tags"
              >Tags: (Separate with commas)</label
            >
          </div>
          <div class="input-field col s6">
            <button
              class="btn waves-effect waves-light"
              type="submit"
              name="action"
            >
              Edit Recipe
            </button>
          </div>
        </div>
      </form>
    </div>
  </body>
</html>
